# Copie para .env (não vai pro GitHub)
VITE_SANITY_PROJECT_ID=isd92ph6
VITE_SANITY_DATASET=production

# CORS (sanity.io/manage → projeto → API → CORS origins):
# http://localhost:5173  http://localhost:5174  + URL do site em produção
